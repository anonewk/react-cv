import React from 'react';
import Navigation from "../components/Navigation";

const Competence = () => {
    return (
        <div>
            <Navigation />
            CCompetence
        </div>
    );
};

export default Competence;
